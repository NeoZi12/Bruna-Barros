# Required only for the seed script (node seed.mjs).
# Get a write-enabled token from:
#   https://sanity.io/manage → Your Project → API → Tokens → Add API token
#   Choose "Editor" or "Deploy Studio" role.
SANITY_TOKEN=your_sanity_write_token_here
